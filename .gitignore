# secrets
.env
.env.*
!.env.example

# generated index — rebuild with `philo ingest`
.philo/

# downloaded source texts cache — rebuild with `philo fetch`
.cache/

# prebuilt index for serverless deploys — commit deliberately with `git add -f`
deploy/index/

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# os
.DS_Store
.vercel
